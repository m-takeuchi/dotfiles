;; (use-package ein
;;   :ensure t
;;   :bind ("C-c C-e" . ein:notebooklist-login)
;;   :config
;;   (setq ein:use-auto-complete t)
;;   ;; Or, to enable "superpack" (a little bit hacky improvements):
;;   ;; (setq ein:use-auto-complete-superpack t)
;;   )
